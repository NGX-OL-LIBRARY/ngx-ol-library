import { NolTransformPipe } from './transform.pipe';

describe('NolTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new NolTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
