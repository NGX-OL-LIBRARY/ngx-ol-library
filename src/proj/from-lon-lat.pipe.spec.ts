import { NolFromLonLatPipe } from './from-lon-lat.pipe';

describe('NolFromLonLatPipe', () => {
  it('create an instance', () => {
    const pipe = new NolFromLonLatPipe();
    expect(pipe).toBeTruthy();
  });
});
