import { NolToLonLatPipe } from './to-lon-lat.pipe';

describe('NolToLonLatPipe', () => {
  it('create an instance', () => {
    const pipe = new NolToLonLatPipe();
    expect(pipe).toBeTruthy();
  });
});
