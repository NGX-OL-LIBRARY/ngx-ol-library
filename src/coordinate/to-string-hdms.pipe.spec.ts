import { NolToStringHDMSPipe } from './to-string-hdms.pipe';

describe('NolToStringHDMSPipe', () => {
  it('create an instance', () => {
    const pipe = new NolToStringHDMSPipe();
    expect(pipe).toBeTruthy();
  });
});
