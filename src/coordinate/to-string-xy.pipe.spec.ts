import { NolToStringXYPipe } from './to-string-xy.pipe';

describe('NolToStringXYPipe', () => {
  it('create an instance', () => {
    const pipe = new NolToStringXYPipe();
    expect(pipe).toBeTruthy();
  });
});
